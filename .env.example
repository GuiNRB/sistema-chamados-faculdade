# Configurações de ambiente para o Sistema de Chamados

# Chave da API do OpenAI (necessária para funcionalidades de IA)
OPENAI_API_KEY=sua-chave-openai-aqui

# String de conexão do banco de dados (opcional - usa SQLite por padrão)
# CONNECTION_STRING=Server=localhost;Database=SistemaChamados;Trusted_Connection=True;Encrypt=False;

# Configurações JWT (opcional - usa valores padrão se não especificado)
# JWT_KEY=sua-chave-jwt-super-secreta-com-pelo-menos-32-caracteres
# JWT_ISSUER=SistemaChamados
# JWT_AUDIENCE=SistemaChamados

# Configurações de email (opcional)
# EMAIL_SMTP_SERVER=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_SENDER_NAME=Suporte Sistema de Chamados
# EMAIL_SENDER_EMAIL=seu-email@gmail.com
# EMAIL_PASSWORD=sua-senha-de-app