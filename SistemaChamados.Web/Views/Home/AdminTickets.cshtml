@{
    ViewData["Title"] = "Gerenciar Chamados";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="app">
  <!-- Sidebar fixa -->
  <aside class="sidebar">
    <img src="~/img/neurohelp-logo2.png" alt="Logo H2O" class="logo" />
    <nav>
      <a href="/Home/AdminDashboard">📊 Painel</a>
      <a href="/Home/AdminTickets" class="active">📋 Chamados</a>
      <a href="/Home/AdminCadastrarTecnico">🧑‍🔧 Cadastrar Técnico</a>
      <a href="/Home/Config">⚙️ Configurações</a>
      <a href="/Home/Index" id="nav-logout">🚪 Sair</a>
    </nav>
  </aside>

  <!-- Área principal -->
  <div class="main">
    <header class="header">
      <h1>Gerenciar Chamados</h1>
    </header>

    <section class="content">
      <div class="card">
        <!-- Filtros -->
        <div class="form-row" style="margin-bottom: 16px">
          <div class="form-group">
            <label for="flt-status-admin">Filtrar por status</label>
            <select id="flt-status-admin">
              <option value="">Todos</option>
            </select>
          </div>

          <div class="form-group">
            <label for="flt-prio-admin">Filtrar por prioridade</label>
            <select id="flt-prio-admin">
              <option value="">Todas</option>
            </select>
          </div>

          <div class="form-group">
            <label for="flt-search-admin">Pesquisar</label>
            <input
              type="text"
              id="flt-search-admin"
              placeholder="Título, ID ou descrição..."
            />
          </div>
        </div>

        <!-- Tabela -->
        <table class="table" id="admin-tickets-table">
          <thead>
            <tr>
              <th>ID</th>
              <th>Título</th>
              <th>Categoria</th>
              <th>Status</th>
              <th>Prioridade</th>
              <th>Técnico</th>
              <th>Ações</th>
            </tr>
          </thead>
          <tbody></tbody>
        </table>
      </div>
    </section>

    <footer class="footer">
      © 2025 Neuro Help — Painel Administrativo
    </footer>
  </div>
</div>