@{
    ViewData["Title"] = "Cadastrar Técnico";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="app">
  <!-- Sidebar fixa -->
  <aside class="sidebar">
    <img src="~/img/neurohelp-logo2.png" alt="Logo H2O" class="logo" />
    <nav>
      <a href="/Home/AdminDashboard">📊 Painel</a>
      <a href="/Home/AdminTickets">📋 Chamados</a>
      <a href="/Home/AdminCadastrarTecnico" class="active">🧑‍🔧 Cadastrar Técnico</a>
      <a href="/Home/Config">⚙️ Configurações</a>
      <a href="/Home/Index" id="nav-logout">🚪 Sair</a>
    </nav>
  </aside>

  <!-- Conteúdo principal -->
  <div class="main">
    <!-- Cabeçalho -->
    <header class="header">
      <h1>Cadastrar Novo Técnico</h1>
      <div class="user-info">Bem-vindo(a)</div>          
    </header>

    <!-- Conteúdo -->
    <section class="content">
      <div class="card">
        <h2 style="margin-bottom: 24px; text-align: center;">Registar Novo Técnico</h2>
        
        <!-- Formulário -->
        <form id="register-tecnico-form">
          <!-- Nome completo -->
          <div class="form-group">
            <label for="t-nome">Nome completo</label>
            <input
              type="text"
              id="t-nome"
              name="t-nome"
              placeholder="Nome completo do técnico"
              required
              autocomplete="name"
            />
          </div>

          <!-- E-mail -->
          <div class="form-group">
            <label for="t-email">E-mail</label>
            <input
              type="email"
              id="t-email"
              name="t-email"
              placeholder="email@empresa.com"
              required
              autocomplete="email"
            />
          </div>

          <!-- Senha -->
          <div class="form-group">
            <label for="t-senha">Senha</label>
            <div class="password-container">
              <input
                type="password"
                id="t-senha"
                name="t-senha"
                placeholder="Senha do técnico"
                required
                autocomplete="new-password"
              />
              <button type="button" class="password-toggle" data-target="t-senha">
                👁️
              </button>
            </div>
          </div>

          <!-- Especialidade -->
          <div class="form-group">
            <label for="t-especialidade">Especialidade (Categoria)</label>
            <select id="t-especialidade" name="t-especialidade" required>
              <option value="">Carregando categorias...</option>
            </select>
          </div>

          <!-- Botão de envio -->
          <button type="submit" class="btn btn-primary">
            Cadastrar Técnico
          </button>
        </form>
      </div>
    </section>

    <!-- Rodapé -->
    <footer class="footer">
      © 2025 Neuro Help — Painel Administrativo
    </footer>
  </div>
</div>